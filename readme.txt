1. prerequsites
    1. python 3.6 and above
    2. pip
2. open terminal and navigate to project dir
3. install project dependencies using following command
    1. pip install -r requirements.txt
4.run the app using following command
    1. uvicorn main:app --reload
5. open browser and hit http://127.0.0.1:8000/docs to open swagger
6. you can test the api
